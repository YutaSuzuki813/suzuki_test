# ファイルコピーバッチスクリプト使用ガイド

## 概要

`copy_files_confirm_allmode.bat` は、リストファイルに記載されたファイルを、指定されたディレクトリから別のディレクトリへ一括コピーするためのバッチスクリプトです。

## 主な機能

- ✅ リストファイルに基づいた一括ファイルコピー
- ✅ デフォルト値の設定により、引数なしでも実行可能
- ✅ 実行前の確認プロンプトで安全性を確保
- ✅ 詳細な処理結果の表示（成功/失敗/スキップ数）
- ✅ コメント行と空行のサポート
- ✅ コピー先ディレクトリの自動作成
- ✅ エラーハンドリングと分かりやすいメッセージ
- ✅ ヘルプ機能（/?, -h, --help）

## 使用方法

### 基本的な使い方

```batch
copy_files_confirm_allmode.bat [コピー元] [コピー先] [リストファイル]
```

### 実行例

#### 1. デフォルト設定で実行（最も簡単）

```batch
copy_files_confirm_allmode.bat
```

デフォルト設定:
- コピー元: `.\02`
- コピー先: `.\06`
- リストファイル: `.\copyfile.txt`

#### 2. コピー元とコピー先を指定

```batch
copy_files_confirm_allmode.bat C:\Source D:\Dest
```

#### 3. すべてのパラメータを指定

```batch
copy_files_confirm_allmode.bat .\data .\backup .\files.txt
```

### ヘルプの表示

```batch
copy_files_confirm_allmode.bat /?
copy_files_confirm_allmode.bat -h
copy_files_confirm_allmode.bat --help
```

## リストファイルの形式

`copyfile.txt` などのリストファイルには、コピーしたいファイルのパスを1行に1つ記述します。

### リストファイルの例

```text
# これはコメント行です（# で始まる行は無視されます）
folder1\file1.txt
folder2\subfolder\file2.doc
data\report.xlsx

# 空行も無視されます
documents\readme.md
images\logo.png
```

### リストファイルのルール

- **相対パス**: パスはコピー元ベースディレクトリからの相対パスで指定
- **コメント**: `#` で始まる行はコメントとして扱われます
- **空行**: 空行は無視されます
- **文字エンコーディング**: Shift-JIS または UTF-8（BOM付き）を推奨

## セットアップ

### 1. デフォルト設定のカスタマイズ

バッチファイルの冒頭部分を編集して、デフォルト値を変更できます：

```batch
set "DEFAULT_SRC_BASE=.\02"
set "DEFAULT_DST_BASE=.\06"
set "DEFAULT_LIST_FILE=.\copyfile.txt"
```

### 2. ディレクトリ構造の準備

```
プロジェクトフォルダ
├── copy_files_confirm_allmode.bat  # このバッチファイル
├── copyfile.txt                     # コピーするファイルのリスト
├── 02/                              # コピー元ディレクトリ（デフォルト）
│   ├── folder1/
│   │   └── file1.txt
│   └── folder2/
│       └── file2.doc
└── 06/                              # コピー先ディレクトリ（自動作成）
```

## 実行の流れ

1. **設定確認**: コピー元、コピー先、リストファイルの設定を表示
2. **検証**: ファイルとディレクトリの存在確認
3. **確認プロンプト**: 実行前に確認を求めます
4. **コピー処理**: リストファイルに従ってファイルをコピー
5. **結果表示**: 成功、失敗、スキップの件数を表示

## 終了コード

- `0`: 正常終了（すべてのファイルが正常にコピーされた）
- `1`: エラー発生またはユーザーによるキャンセル

## トラブルシューティング

### エラー: コピー元ディレクトリが存在しません

**原因**: 指定されたコピー元ディレクトリが存在しない

**解決策**:
- ディレクトリパスが正しいか確認
- 相対パスの場合、バッチファイルの実行場所を確認

### エラー: リストファイルが存在しません

**原因**: 指定されたリストファイルが見つからない

**解決策**:
- リストファイルのパスが正しいか確認
- リストファイルを作成する

### エラー: ファイルが見つかりません

**原因**: リストファイルに記載されたファイルがコピー元に存在しない

**解決策**:
- リストファイルのパスが正しいか確認
- コピー元ディレクトリにファイルが存在するか確認

### 一部のファイルのコピーに失敗

**原因**: 
- ファイルが使用中である
- 権限がない
- ディスク容量不足

**解決策**:
- ファイルを使用しているアプリケーションを閉じる
- 管理者権限で実行する
- ディスク容量を確認する

## 使用例シナリオ

### シナリオ1: 日次バックアップ

毎日特定のファイルをバックアップディレクトリにコピーする場合：

```batch
REM daily_backup.bat
@echo off
copy_files_confirm_allmode.bat C:\WorkData D:\Backup\%DATE% backup_list.txt
```

### シナリオ2: プロジェクトファイルの配布

プロジェクトの特定のファイルを別の環境に配布する場合：

```batch
copy_files_confirm_allmode.bat .\project \\server\share\project deploy_list.txt
```

### シナリオ3: 開発環境から本番環境へのデプロイ

```batch
copy_files_confirm_allmode.bat .\dev .\production production_files.txt
```

## カスタマイズのヒント

### 1. ログファイルの出力

処理結果をログファイルに保存する場合：

```batch
copy_files_confirm_allmode.bat > copy_log.txt 2>&1
```

### 2. タスクスケジューラとの連携

Windowsのタスクスケジューラで自動実行する場合、確認プロンプトをスキップするバージョンを作成することを検討してください。

### 3. 複数のリストファイルを使用

異なる用途に応じて複数のリストファイルを用意：

```batch
copy_files_confirm_allmode.bat .\src .\dst documents.txt
copy_files_confirm_allmode.bat .\src .\dst images.txt
copy_files_confirm_allmode.bat .\src .\dst configs.txt
```

## 注意事項

- ⚠️ 同名のファイルがコピー先に存在する場合、上書きされます（確認なし）
- ⚠️ 大量のファイルをコピーする場合、処理に時間がかかる可能性があります
- ⚠️ ネットワークドライブへのコピーは、ネットワークの状態により失敗する可能性があります
- ⚠️ 日本語を含むパスを使用する場合、文字コードに注意してください

## ライセンス

このスクリプトは自由に使用・改変できます。

## サポート

問題が発生した場合は、以下の情報を含めて報告してください：
- エラーメッセージ
- 実行環境（Windows バージョン）
- 実行時のパラメータ
- リストファイルの内容（該当部分）
